/// <reference types="react" />
import { ImageDialogProps } from "../../types/dialog";
declare const ImageDialog: ({ handleAddClick, handleClose, dialogOpen, image, setImage, }: ImageDialogProps) => JSX.Element;
export default ImageDialog;
